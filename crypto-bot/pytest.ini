[pytest]
testpaths = tests
pythonpath = src
markers =
    network: needs outbound access to public exchange APIs
    integration: runs against the pinned freqtrade build
filterwarnings =
    ignore::DeprecationWarning
